# ── 인증 + 프로젝트 데이터 (Firebase Firestore) ────────────────
# 계정(pms_users)과 프로젝트(pms_projects) 모두 Firestore에 저장. 별도 인증 서비스 없음.
# Firebase 콘솔 → 프로젝트 설정 → '내 앱'(웹) → SDK 설정/구성에서 복사
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:xxxxxxxxxxxx

# ── 첨부파일 (Cloudflare R2 presign Worker) ────────────────────
# worker/ 를 배포한 뒤의 Worker URL. 미설정 시 dataURL(base64) 폴백으로 동작.
VITE_R2_WORKER_URL=https://pms-r2-presign.your-subdomain.workers.dev

# ── 동사무소 게시판 프록시 (Cloudflare Worker) ──────────────────
# 브라우저는 CORS 때문에 게시판 API를 직접 호출할 수 없다.
# 이 값을 설정하면 Worker가 서버 측에서 대신 호출한다(자격증명도 Worker 시크릿에 보관).
# 미설정 시 브라우저 직접 호출 모드 — 게시판이 CORS를 허용해야만 동작한다.
#
# Worker 쪽 설정: worker/wrangler.toml 의 OFFICE_BASE_URL / OFFICE_POST_PATH +
#   wrangler secret put OFFICE_USERNAME
#   wrangler secret put OFFICE_PASSWORD
VITE_OFFICE_PROXY_URL=https://pms-r2-presign.your-subdomain.workers.dev
