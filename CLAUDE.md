# PMS (pms.sanghak.kr) — 반응형 / 모바일 UI 표준

스택: React (JSX) \+ Tailwind CSS. 이 프로젝트는 PC 대시보드(다중 패널·대형 데이터·칸반 보드)가 핵심이며, 모바일 대응은 이미 상당 부분 구현되어 있다. 아래 규칙은 그 기반을 **표준으로 고정**하고, 현재 드러난 문제(정보 밀도 저하)를 교정하기 위한 것이다.

## 최우선 원칙

**모바일에서 사용자가 가능한 한 많은 기능·정보를 한 화면에서 보고 조작할 수 있어야 한다.** 스크롤 길이를 줄이고 정보 밀도를 높이는 방향이 항상 우선한다. 단, 가독성과 터치 조작성을 해치지 않는 선에서.

## 현재 진단 (실측 기준, 반드시 교정)

- 본문 폰트가 과도하게 작다(측정상 9px 수준). 반면 컨테이너 높이는 폰트의 약 5배(44px)로 빈 패딩이 과하다.  
- 그 결과 전체 페이지가 한 화면의 8배 이상 길이가 되어 "한눈에"가 불가능하다.  
- **문제는 폰트와 컨테이너 사이징의 비율 불일치다. 폰트는 키우고, 컨테이너 패딩/높이는 줄여 밀도를 높인다.**

## 폰트 규칙 (모바일)

- 본문/입력값/리스트 텍스트: 최소 `text-sm`(14px). 9\~12px 금지.  
- 핵심 숫자(KPI 수치), 화면 제목: 오히려 키운다 (`text-lg`\~`text-2xl`).  
- 보조 라벨(분류, 상태 뱃지 등): 최소 `text-xs`(12px), 그 이하 금지.  
- 폰트를 줄여서 공간을 확보하려 하지 말 것. 공간은 패딩·여백·레이아웃 압축으로 확보한다.

## 컨테이너 / 패딩 / 밀도 규칙 (모바일)

- PC 기준의 큰 패딩·높이를 모바일에 그대로 쓰지 말 것.  
- **버튼·패널·리스트 항목의 패딩은 작게 하되 바닥까지 줄이지 말 것.** 답답하거나 터치가 불편하면 안 된다.  
  - 세로 패딩: `py-1.5py-2` ~~(6~~8px) 권장. `py-4` 같은 PC값 금지, `py-0`도 금지.  
  - 좌우 패딩: `px-2.5`\~`px-3` 권장.  
  - 텍스트와 컨테이너 가장자리 사이 여백도 같은 감각으로 좁힌다.  
- **터치 타겟은 패딩이 아니라 최소 높이로 보장한다.** 패딩으로 빈 높이를 만들지 말고, 버튼/탭 등 조작 요소에 `min-h-9`(36px) 이상을 따로 줘서 "보이는 틀은 작지만 누르기는 편한" 상태로 만든다.  
- 리스트 항목 사이 간격(`gap`, `mb`)도 최소화하여 한 화면에 더 많은 항목이 보이게.  
- 목표 감각: 14px 본문 기준 행 높이는 폰트의 2~~2.5배(약 32~~40px) 내로. 현재의 5배(44px)는 과하다.

## 필터 / 도구모음 (프로젝트 현황 화면 — 가장 시급)

- 현재 필터 6개(단계/담당팀/우선순위/유형/서비스 \+ CSV)가 각각 풀폭·풀높이 박스로 세로 나열되어 화면을 점유, 정작 리스트가 한참 밀려난다.  
- 모바일에서는 필터를 펼쳐두지 말 것. **"필터" 버튼 1개로 접고, 탭하면 bottom sheet / 드로어로 전체 필터를 띄운다.**  
- 적용된 필터는 칩(chip) 형태로 한 줄에 압축 표시.  
- 검색창은 항상 노출하되 높이를 적정화(터치 타겟은 유지, 빈 패딩은 제거).  
- 이 변경만으로도 리스트가 첫 화면에 바로 들어와야 한다.

## 화면별 모바일 레이아웃 표준 (이미 구현된 패턴 — 유지·일관 적용)

- **사이드바 → 하단 탭바**: 역할(role)에 따라 탭 개수가 다르다.  
  - 관리자: 5개 — 대시보드 / 새 요청 / 프로젝트 현황 / 흐름도 / 설정.  
  - 그 외 역할: 4개 — 위에서 **설정 제외**(관리자 전용).  
  - 탭바는 role 기준으로 동적 렌더링. 탭 개수가 4/5로 바뀌어도 가로 정렬·간격이 균등하게(`flex-1` 균등 분배) 유지되어야 한다. 5개일 때도 라벨이 줄바꿈·잘림 없이 들어가도록 폰트·아이콘 크기 조정.  
  - 신규 화면도 이 탭바 구조를 따른다.  
- **KPI 카드**: PC 가로 1줄 → 모바일 2열 그리드 유지. 단, 카드 높이를 줄이고 숫자 폰트를 키워 밀도 개선.  
- **칸반 6단계 흐름 보드**: PC 가로 → 모바일 세로 스택 유지. 단계 헤더는 sticky 검토.  
- 신규 복잡 화면도 위 전환 규칙(가로→세로 스택, 사이드→탭바)을 동일하게 적용.

## SRS / SDS 2단 에디터 (확정: A안 — 탭 토글 편집)

- PC: 좌(SRS) / 우(SDS) 2단 동시 노출 \+ 리치텍스트 툴바.  
- 모바일: **SRS / SDS 탭 토글로 1열씩 전환하며 편집까지 지원한다.** (조회 전용 아님)  
  - 화면 상단에 SRS·SDS 세그먼트 탭을 두고, 선택된 쪽만 1열 풀폭으로 표시.  
  - 현재 모바일에서 프로젝트 선택 시 상세 에디터가 노출되지 않는 문제를 먼저 해결할 것(리스트→상세 진입 경로 복구).  
  - 리치텍스트 툴바는 모바일 폭에 맞게 줄바꿈 또는 가로 스크롤 처리, 핵심 버튼 우선 노출.

## 단위 / 공통

- 고정 px 폭 지양: `%`, `flex`, `grid`, `max-w-*` 사용.  
- 이미지 `max-w-full h-auto`.  
- 터치 타겟 최소 44x44px — 단, 이는 **터치 가능 영역**이지 "텍스트 한 줄에 44px 높이를 줘라"가 아니다. 패딩으로 빈 높이를 만들지 말고, 작은 컨트롤은 투명 히트영역으로 확장.  
- 페이지 전체 가로 스크롤 금지(가로 스크롤은 테이블 컨테이너 내부로만 한정).  
- viewport-fit=cover 유지(노치 대응). 하단 탭바는 safe-area inset 반영.

## 작업 방식

- 복잡한 화면을 한 번에 다 고치지 말 것.  
  1) 영향 컴포넌트와 현재 폰트/패딩 값을 진단·보고만 한다(코드 수정 X).  
  2) 사용자 승인 후 컴포넌트 단위로 하나씩 수정.  
- 수정 후 데스크톱(`lg:` 이상)이 그대로인지 항상 확인.

## 검증 기준

- Chrome DevTools 디바이스 모드: iPhone(390px) / Galaxy S(360px) / iPad(768px) / Desktop(1280px).  
- 모바일에서 가로 스크롤 없음, 본문 14px 이상, 첫 화면에 핵심 콘텐츠(리스트/KPI)가 바로 보일 것.  
- 전체 페이지 세로 길이가 현재(한 화면 8배)보다 유의미하게 짧아질 것.

